{
  "name": "hsl-web",
  "version": "3.0.0",
  "private": true,
  "type": "module",
  "description": "The HeatSync Labs API, identity provider and door controller service",
  "license": "MIT",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "migrate": "node scripts/migrate.ts",
    "import": "node scripts/import.ts",
    "test": "npm --prefix api test && npm --prefix door test",
    "typecheck": "tsc -p scripts/tsconfig.json && npm --prefix api run typecheck && npm --prefix door run typecheck"
  },
  "dependencies": {
    "postgres": "3.4.9"
  },
  "devDependencies": {
    "@types/node": "24.13.4",
    "typescript": "7.0.2"
  }
}

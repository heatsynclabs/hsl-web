# Decisions

One file per architectural or dependency decision, numbered in order. Half a page
each. The point is that in three years someone can see the choice was considered
rather than defaulted into.

Reversing a decision is fine. Write a new ADR that supersedes the old one and say
what changed.

Start from `0000-template.md`.

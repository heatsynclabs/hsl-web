# Copy to .env on the lab host.

# The door controller on the lab LAN. Confirm the live address on the host;
# 192.168.1.177 is the default from the public example config.
CONTROLLER_URL=http://192.168.1.177

# The public members API this service reaches out to. Outbound only.
API_URL=https://members.heatsynclabs.org
